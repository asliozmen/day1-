package arrays;

public class ArraysWithTemirlan {

	public static void main(String[] args) {
		//Declare an array of String with size of 5
		//assign values to only 4 elements and print out
		
		String [] array= new String[5];
		 array[0]="Irfan";
		 array[1]="Fatih";
		 array[2]="Meryem";
		 array[3]="Vehbi";
		 
		 for (int i=0; i< array.length; i++) {
		 
		 System.out.println(array[i]); // for (i=0; i>arr.length; i++) {  // if (arr[i] == null) ve cikar evde.
		 
	}

}}
